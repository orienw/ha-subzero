[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py314"
line-length = 100
extend-exclude = [".private", ".research", ".venv"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP"]
